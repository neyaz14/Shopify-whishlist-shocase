// import { authenticate as shopifyAuthenticate } from "@shopify/shopify-app-remix/server";


// export const authenticate = {
//   admin: async (request) => {
//     try {
//       const session = await shopifyAuthenticate.admin(request);
//       return { session };
//     } catch (error) {
//       throw new Response(null, { status: 401 });
//     }
//   },
// };
